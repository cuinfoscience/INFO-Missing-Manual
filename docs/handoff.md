# Hand-off note

**Updated 2026-09-24,** after #36 merged and the #30 fix and the screenshot pilot were written. This note describes the present state. Rewrite it when a session stops or the state changes, and don't let it grow into a history: history lives in git, in [`decisions.md`](decisions.md), and in the AARs.

## Where things stand

- **The book** has 39 chapters and appendices in seven parts, and renders with zero warnings.
- **Merged 2026-09-24:**
  - #35: `AGENTS.md` and this `docs/` folder; supporting code moved into `tools/`, with the screenshot toolkit and the layout audit; `CONTRIBUTING.md`; and the plans for screenshots, #30, and #34.
  - #36: chapter 9's section on how much space programming tools take, and chapter 10's section on cloud storage (`@sec-filesystem-cloud`). It closed #34.
- **Open pull request from `claude/markdown-chapter-expansion-difpcc`,** closing #30, in two commits:
  - **#30:** each chapter's meme now heads the right sidebar, above the table of contents (`margin-header`, generated by `tools/chapter-meme/sync_margin_header.py` and checked in CI), with an inline copy on narrow screens. The table of contents is open at load on 41 of 41 pages, up from 4. The issue-forms sync now skips front matter, whose generated `# margin-header:` comment it had read as each chapter's title.
  - **The screenshot pilot (plan M1):** three placeholders replaced with real captures, taken from a pinned local JupyterLab (`tools/shots/fixtures/jupyter/`). They are the two JupyterLab figures in `jupyter` and the rendered DataFrame in `pandas-basics`. The toolkit gained a headless `between` crop (selftest 69 of 69). The lessons are in "Patterns and pitfalls" in `tools/shots/README.md`, `AGENTS.md` ("Screenshots"), and the [screenshot AAR](aar/AAR_INFO-Missing-Manual_2026-09-24.md).

## Waiting on the maintainer

| Item | What to decide |
|---|---|
| The open pull request | Review and merge (agents don't merge unless asked). Look at a chapter or two at desktop and phone widths: the sticky sidebar and the meme's 15rem size were the #30 plan's defaults. |
| Wider columns for screenshots ([plan](plans/2026-09-24-screenshots.md) §8, item 9) | The pilot's JupyterLab overview sits in `column-page-inset-right`, the only column where its text passes at 1024 px, and it covers the table of contents while on screen. Accept that for the few figures that need it, or require a crop to 800 px? |
| The other screenshot decisions (plan §8) | Before M2's web captures: the capture identity (User-Agent). Later: how to capture VS Code; the source of a real review thread; the Actions run view; "Illustration" in the terminal figures' captions; deleting eight orphaned images; the merge policy. The pilot chapter is settled (`jupyter`). |
| CI for the issue forms | Run `tools/issue-forms/sync_issue_chapters.py --check` in CI, as the margin-header check is? Run by hand, it caught a regression in the #30 work before the push. |
| Chapter 10's trailing sections | Fold "downloaded from Canvas" and "unzip" into the body (a review follow-up in [`roadmap.md`](roadmap.md)). |
| Community files | A `CODE_OF_CONDUCT.md` (for example, the Contributor Covenant). `CONTRIBUTING.md` has a short "Be kind" section, but no formal policy exists. |

## Known issues

- **Nine `PLACEHOLDER-*` images** don't exist, in seven chapters (`version-control` 2, `operating-system` 2, and one each in `automation`, `collaboration`, `http-apis`, `linting`, `text-editors`), so those figures render broken. The screenshot plan covers them (M2–M5).
- **The published book** still hides the table of contents at load on 37 of 41 pages until the open pull request merges.
- **The JupyterLab overview figure** folds the table of contents into its toggle while the figure is on screen (Quarto's behavior for wide columns; see the decision above).
- **`tools/shots/run doctor <chapter>`** warns `localhost: not reachable now` for a chapter captured from a fixture, running or not. Ignore it; the fixture's `start.sh` confirms the server answers. Fixing `doctor` is an open action in the screenshot AAR.
- **Old `/parts/...` chapter URLs 404,** by design ([`decisions.md`](decisions.md), 2026-08-28).
- **Quarto 1.9 can't build the book locally:** 1.9.15 rejects `website: llms-txt` in `_quarto.yml`. The docs now say 1.10 or later. `AGENTS.md`'s note that `llms-txt` only works under `website:` in Quarto 1.9 predates this finding and hasn't been re-checked against 1.10.

## Notes for cloud sessions

- **Quarto** isn't installed system-wide. Download a 1.10 release into the session's scratch directory. With 1.9.x, comment out the `website:` block to render, and restore `_quarto.yml` exactly afterwards.
- **`tools/shots`:** `bash tools/shots/bootstrap.sh --headed --tex` works in the container (Chrome for Testing 154 comes through Selenium Manager); then `tools/shots/run doctor`. Never run `playwright install`, and never turn off TLS verification. HTTPS goes through the session's proxy. `tools/layout-audit/` runs on the toolkit's Python.
- **JupyterLab:** use the fixture, `bash tools/shots/fixtures/jupyter/start.sh`, rather than starting it by hand. It installs the pinned packages on first run, passes `--allow-root` when run as root, turns off news and update prompts, and serves on `127.0.0.1:8899`. Stop it with `stop.sh` when the takes are done.
- **Chrome and long paths:** don't point `TMPDIR` at the long scratch path; Chrome's `SingletonSocket` path overflows. VS Code needs a short `--user-data-dir` for the same reason (a 107-character limit), and an unset `NODE_OPTIONS`.
- **`pkill -f <pattern>`** also matches the shell running it; use `pgrep -f '[j]upyter'` and kill each PID.
- **Merged branches can't be deleted** from a cloud session (the git proxy refuses). This session reuses one branch, restarted from `main` after each merge.

## Next, when work resumes

1. Merge the open pull request (#30 and the pilot) if the maintainer approves it.
2. Settle the screenshot decisions above, above all item 9 (wider columns) and the capture identity, and record each in [`decisions.md`](decisions.md).
3. Screenshot milestone M2, the web pages: `version-control` (2), `http-apis` (1), and `automation` (1), one chapter per pull request. Read "Patterns and pitfalls" in `tools/shots/README.md` first, and add to it what each chapter teaches.
4. Teach `doctor` to check a fixture chapter's local server instead of `https://localhost/robots.txt` (screenshot AAR, actions).
5. A CI check for screenshots (plan M6): `tools/shots/run check` on pull requests that touch `graphics/` or the recipes.
6. Re-check the "At CU Boulder" callouts in chapters 9 and 10 against OIT's pages each fall; update the month in their headings.
