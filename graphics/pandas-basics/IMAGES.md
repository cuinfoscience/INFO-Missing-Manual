# Images for pandas-basics

Figures in `chapters/pandas-basics.qmd`. `tools/shots` writes the table below from `provenance.json`
and rewrites only what is between the two markers. Notes below the table are
for people: what a figure shows that is easy to miss, and what a retake needs.

<!-- shots:begin: generated from provenance.json by tools/shots; edits between these markers are replaced -->
| File | Kind | Captured | Source | How |
|---|---|---|---|---|
| `dataframe-render.png` | capture | 2026-09-24 | http://localhost:8899/lab/tree/notebooks/dataframe.ipynb?reset | tools/shots: Google Chrome for Testing 154.0.8037.57, 800×600 at 2× |
<!-- shots:end -->

## Notes

From the local JupyterLab fixture, [`tools/shots/fixtures/jupyter/`](../../tools/shots/fixtures/jupyter/), notebook `notebooks/dataframe.ipynb`, which builds the chapter's own DataFrame. Retake it after the `jupyter` figures; see [`../jupyter/IMAGES.md`](../jupyter/IMAGES.md).

- The recipe clicks the Markdown cell below the code cell, so the active cell's blue border is outside the crop; the crop has no bottom pad for the same reason.
