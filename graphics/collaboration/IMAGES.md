# Images for collaboration

Figures in `chapters/collaboration.qmd`. `tools/shots` writes the table below from `provenance.json`
and rewrites only what is between the two markers. Notes below the table are
for people: what a figure shows that is easy to miss, and what a retake needs.

<!-- shots:begin: generated from provenance.json by tools/shots; edits between these markers are replaced -->
| File | Kind | Captured | Source | How |
|---|---|---|---|---|
| `github-pr-review-comment.png` | capture | 2026-09-25 | https://github.com/cuinfoscience/INFO-Missing-Manual/pull/47#discussion_r4100343519 | tools/shots: Google Chrome for Testing 154.0.8037.57, 1024×900 at 2× |
<!-- shots:end -->

## Notes

- **`github-pr-review-comment`** is the whole resolved thread from `version-control`'s `github-pr-diff`: the comment, its suggested change, and the reply. Written by Claude Code from the maintainer's account; names and avatars blurred. At 536×675 it is taller than 600 (`relaxed:` in the recipe). Opened by its permalink, GitHub shows the resolved thread expanded; don't change the URL to the plain pull request page, where it collapses.
