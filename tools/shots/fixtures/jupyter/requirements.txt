# The JupyterLab the book's Jupyter screenshots show, pinned so a retake shows
# the same interface. start.sh installs these into .venv beside this file.
jupyterlab==4.6.4
ipykernel==7.3.0
nbconvert==7.17.1
pandas==3.0.6
numpy==2.4.6
